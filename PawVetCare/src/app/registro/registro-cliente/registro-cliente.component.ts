import { Component } from '@angular/core';

// Decorador que define el componente 'app-registro-cliente'
@Component({
  selector: 'app-registro-cliente', // Nombre del selector que se usará en HTML
  templateUrl: './registro-cliente.component.html', // Ruta del archivo HTML que define la plantilla del componente
  styleUrls: ['./registro-cliente.component.css'] // Ruta del archivo CSS que contiene los estilos del componente
})
// Clase que representa el componente 'RegistroClienteComponent'
export class RegistroClienteComponent {
  // Esta clase está vacía actualmente, pero aquí se pueden agregar propiedades y métodos para manejar la lógica del registro de clientes
}