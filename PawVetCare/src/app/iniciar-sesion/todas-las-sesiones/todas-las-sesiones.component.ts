import { Component } from '@angular/core';

@Component({
  // Selector para el componente, se utilizará como etiqueta HTML en otros templates
  selector: 'app-todas-las-sesiones',
  
  // Ruta al archivo de plantilla HTML que define la vista del componente
  templateUrl: './todas-las-sesiones.component.html',
  
  // Ruta al archivo CSS que contiene los estilos específicos para este componente
  styleUrls: ['./todas-las-sesiones.component.css']
})

// Clase del componente, define su lógica y comportamiento
export class TodasLasSesionesComponent {
  // Aquí se pueden agregar propiedades y métodos que se utilizarán en la plantilla
}
